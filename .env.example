# Copy to .env at the repository root (gitignored).
# Used by reviewer-runner (npm run review) and evals (npm run eval).

CURSOR_API_KEY=

# Optional eval overrides (default: composer-2.5)
# EVAL_MODEL_ID=
# EVAL_JUDGE_MODEL_ID=
